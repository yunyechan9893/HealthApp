
    * 2022.11.11
        - 메인 프레임에서 프레그먼트에 커스텀 리스트 값을 넘겨주고 싶은데, 방법이 없을까?(완료)
        - 데이터베이스에 접근을 최소화 하고 싶어 필요한 뷰 값을 앱을 시작할 때 한번에 받아와 그 값을 활용하고 싶다.(완료)

    * 2022.11.12
        - ResultSet에 받아온 여러개의 값을 어떻게 액티비티에 넘겨주지 ?? 도저히 모르겠네..(완료)

        -- 핸들러 메세지를 bool값으로 반환 하고, 메인 스레드에서 메세지를 받으면 GetList를 통해 값을 받아오고
        -- 그 후 SharedPreferences에 리스트 값을 넣어줬다.
        -- 어떤 액티비티에서든 데이터 접근이 가능해졌다.

    * 2022.11.13
        # 21:13 #
        - Fragment 에서 dialog를 띄우기 위해 DialogFragment를 만들어 화면에 inflate 해주었다.(완료)
        - 하지만 화면에 표시되지 않는다. (inflate가 제대로 되지 않고 있는 듯)(완료)

    * 2022.11.18
        # 15:14 #
        - 식단 등록 페이지에 필요한 저장프로시저 작성(완료)
        - 프로시저와 연동(완료)
        - 등록 버튼에서 에러가 났다. 같은 핸들러를 넘겨주다보니 popup menu가 자꾸 show 된다. 이따 와서 고쳐주자(완료)

        -- 핸들러 에러가 아닌 String 값을 잘못지정해줬다.
        -- 에러 해결 완료

    * 2022.11.19
        # 15:08 #
        - 쉐어러프리퍼런스의 한계를 경험했다. 삭제 기능이 너무 불편하다.(완료)
        - 값을 잘 가져오지 못하는건지 값 삭제가 이상한건지 쉐어 파일 값이 이상하게 저장된다.(완료)

        -- 쉐어러프리퍼런스에서 clear를 사용하려면 commit을 반드시 써줘야한다.
        -- 이거 하나로 저장 기능 오류를 다 잡았다.

    * 2022.11.20
        # 17:40 #
        - 프레그먼트 갱신 수정하기
        - Management 페이지 칼로리 출력 고치기(완료)
        - dietList에서 삭제, 수정 구현하기

    * 2022.11.26
       # 22:26 #
       - 테이블, 저장프로시저, 안드로이드에 오늘의 목표 칼로리 구현 (완료)
       - 하단네비게이션 누를 시 프레그먼트 갱신하기(완료)

        -- remove는 프레그먼트 deps 깊이가 2개만 돼도 삭제 동작을 안한다.
        -- 그러므로 PopBackStack을 이용하여 종료 시켜주어야한다. (생명주기 Destroy 실행 됨)

    * 2022.11.27
        # 16:28 #
        오늘 한 일
        - 프레그먼트 갱신을 완료하였다.(완료)
        - 엉망인 코드들을 어느 정도 정리해주었다.(완료)

        해야할 일
         >> dietList에서 삭제, 수정 구현하기

    * 2022.11.29
        # 20:10 #
        오늘 한 일
        - Diet_Detail 화면 디자인, 리스트 디자인(완료)
        - Diet에서 List 값을 넘겨주는 intent 코드 작성(완료)
        - 넘겨받은 값으로 no값을 찾아내는 코드 작성(완료)
        - Diet 리스트 버그 해결(완료)

        해야할 일
         >> dietList에서 삭제, 수정 구현하기

    * 2022.11.29
         #  #
         오늘 한 일
         -


         해야할 일
          >> 다른아이디로 오늘목표 설정 안되는 버그 발견